apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: grafana
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host("grafana.staging.mchill.duckdns.org")
      kind: Rule
      services:
        - name: loki-grafana
          port: 80
