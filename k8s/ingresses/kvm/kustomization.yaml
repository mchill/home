namespace: server
resources:
  - endpoint.yaml
helmGlobals:
  chartHome: ../../charts
helmCharts:
  - name: ingress
    valuesInline:
      app: kvm
      service:
        disableSelector: true
      ingress:
        routes:
          - match: Host("kvm.mchill.io")
            service:
              scheme: https
