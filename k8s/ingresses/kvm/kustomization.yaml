namespace: server
resources:
  - endpoint.yaml
helmGlobals:
  chartHome: ../../charts
helmCharts:
  - name: ingress
    valuesInline:
      app: kvm
      addSelector: false
      routes:
        - match: Host("kvm.mchill.io")
          scheme: https
