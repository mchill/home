namespace: server
resources:
  - endpoint.yaml
helmGlobals:
  chartHome: ../../charts
helmCharts:
  - name: ingress
    valuesInline:
      app: pdu
      service:
        disableSelector: true
      ingress:
        routes:
          - match: Host("pdu.mchill.io")
