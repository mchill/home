namespace: server
resources:
  - endpoint.yaml
helmGlobals:
  chartHome: ../../charts
helmCharts:
  - name: ingress
    valuesInline:
      app: pdu
      addSelector: false
      routes:
        - match: Host("pdu.mchill.io")
