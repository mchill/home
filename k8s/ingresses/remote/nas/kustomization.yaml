namespace: server
resources:
  - endpoint.yaml
helmGlobals:
  chartHome: ../../../charts
helmCharts:
  - name: ingress
    valuesInline:
      app: remote-nas
      addSelector: false
      routes:
        - match: Host("nas.remote.mchill.io")
