apiVersion: v1
kind: Service
metadata:
  name: metallb-test
  annotations:
    metallb.universe.tf/allow-shared-ip: external
spec:
  type: LoadBalancer
  selector:
    app: dashboard
  ports:
  - port: 8080
    targetPort: http
