apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: server
resources:
  - deployment.yaml
helmGlobals:
  chartHome: ../../charts
helmCharts:
  - name: ingress
    valuesInline:
      app: homeassistant
      ingress:
        routes:
          - match: Host("home.mchill.io")
            disableForwardAuth: true
      loadBalancer:
        ip: 192.168.1.81
        port: 8123
  - name: persistence
    valuesInline:
      app: homeassistant
      size: 1Gi
