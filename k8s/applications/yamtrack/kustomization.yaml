namespace: server
resources:
  - deployment.yaml
  - sealed-secret.yaml
helmGlobals:
  chartHome: ../../charts
helmCharts:
  - name: ingress
    valuesInline:
      app: yamtrack
      routes:
        - match: Host("yamtrack.mchill.io")
  - name: persistence
    valuesInline:
      app: yamtrack
      size: 128Mi
