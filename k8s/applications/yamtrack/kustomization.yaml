apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: server
resources:
  - deployment.yaml
  - sealed-secret.yaml
helmGlobals:
  chartHome: ../../charts
helmCharts:
  - name: ingress
    valuesInline:
      app: yamtrack
      ingress:
        routes:
          - match: Host("yamtrack.mchill.io")
  - name: persistence
    valuesInline:
      app: yamtrack
      size: 128Mi
