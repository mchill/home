kind: Deployment
apiVersion: apps/v1
metadata:
  name: plex
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    spec:
      nodeName: matthews-laptop
      securityContext:
        fsGroup: 911
      containers:
        - name: plex
          image: ghcr.io/linuxserver/plex:1.30.1
          ports:
            - name: ingress
              containerPort: 32400
            - name: gdm-1
              containerPort: 32410
              protocol: UDP
            - name: gdm-2
              containerPort: 32412
              protocol: UDP
            - name: gdm-3
              containerPort: 32413
              protocol: UDP
            - name: gdm-4
              containerPort: 32414
              protocol: UDP
          env:
            - name: TZ
              value: America/New_York
            - name: ADVERTISE_IP
              value: http://192.168.1.246:32400,https://plex.mchill.io
          resources:
            limits:
              cpu: 4
              memory: 2Gi
            requests:
              cpu: 1
              memory: 512Mi
          volumeMounts:
            - name: plex
              mountPath: /config
            - name: media
              mountPath: /data/movies
              subPath: movies
            - name: media
              mountPath: /data/movies-uhd
              subPath: movies-uhd
            - name: media
              mountPath: /data/extras
              subPath: extras
            - name: media
              mountPath: /data/tv
              subPath: tv
            - name: media
              mountPath: /data/tv-uhd
              subPath: tv-uhd
      volumes:
        - name: plex
          persistentVolumeClaim:
            claimName: plex
        - name: media
          persistentVolumeClaim:
            claimName: media
