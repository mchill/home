apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: server
resources:
  - deployment.yaml
helmGlobals:
  chartHome: ../../charts
helmCharts:
  - name: ingress
    valuesInline:
      app: peanut
      ingress:
        routes:
          - match: Host("peanut.mchill.io")
  - name: persistence
    valuesInline:
      app: peanut
      size: 128Mi
