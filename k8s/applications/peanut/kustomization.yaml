namespace: server
resources:
  - deployment.yaml
helmGlobals:
  chartHome: ../../charts
helmCharts:
  - name: ingress
    valuesInline:
      app: peanut
      ingress:
        routes:
          - match: Host("peanut.mchill.io")
  - name: persistence
    valuesInline:
      app: peanut
      size: 128Mi
