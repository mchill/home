apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: server
resources:
  - cron-job.yaml
configMapGenerator:
  - name: kometa-permissions
    files:
      - config/config.yaml
      - config/movies.yml
      - config/tv.yml
