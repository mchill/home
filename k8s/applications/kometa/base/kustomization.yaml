apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: server
resources:
  - sealed-secret.yaml
helmGlobals:
  chartHome: ../../../charts
helmCharts:
  - name: persistence
    valuesInline:
      app: kometa
      size: 512Mi
