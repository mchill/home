apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: server
resources:
  - deployment.yaml
helmGlobals:
  chartHome: ../../charts
helmCharts:
  - name: ingress
    valuesInline:
      app: sonarr
      ingress:
        routes:
          - match: Host("sonarr.mchill.io")
  - name: persistence
    valuesInline:
      app: sonarr
      size: 1Gi
