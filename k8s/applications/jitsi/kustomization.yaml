apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: server
resources:
  - config-map.yaml
  - deployment.yaml
  - sealed-secret.yaml
  - service.yaml
helmGlobals:
  chartHome: ../../charts
helmCharts:
  - name: ingress
    valuesInline:
      app: jitsi
      ingress:
        routes:
          - match: Host("jitsi.mchill.io")
            disableForwardAuth: true
  - name: persistence
    valuesInline:
      app: jitsi
      size: 128Mi
