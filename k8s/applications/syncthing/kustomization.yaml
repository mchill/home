namespace: server
resources:
  - deployment.yaml
helmGlobals:
  chartHome: ../../charts
helmCharts:
  - name: ingress
    valuesInline:
      app: syncthing
      ingress:
        routes:
          - match: Host("syncthing.mchill.io")
  - name: persistence
    valuesInline:
      app: syncthing
      size: 256Mi
