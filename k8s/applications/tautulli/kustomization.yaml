apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: server
resources:
  - deployment.yaml
helmGlobals:
  chartHome: ../../charts
helmCharts:
  - name: ingress
    valuesInline:
      app: tautulli
      ingress:
        routes:
          - match: Host("tautulli.mchill.io")
      loadBalancer:
        ip: 192.168.1.83
        port: 80
  - name: persistence
    valuesInline:
      app: tautulli
      size: 1Gi
