apiVersion: v1
kind: Service
metadata:
  name: nut-exporter
spec:
  type: ClusterIP
  selector:
    app: nut-exporter
  ports:
    - name: nut-exporter
      port: 9995
      targetPort: nut-exporter
