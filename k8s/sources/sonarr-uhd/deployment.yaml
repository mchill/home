apiVersion: apps/v1
kind: Deployment
metadata:
  name: sonarr-uhd
spec:
  selector:
    matchLabels:
      app: sonarr-uhd
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: sonarr-uhd
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: sonarr
          image: ghcr.io/linuxserver/sonarr:4.0.19
          ports:
            - name: ingress
              containerPort: 8989
          env:
            - name: TZ
              value: America/New_York
            - name: PUID
              value: "1000"
            - name: PGID
              value: "1000"
            - name: MAX_NAME
              value: "140"
          volumeMounts:
            - name: sonarr-uhd
              mountPath: /config
            - name: hddpool2-media
              mountPath: /tv-uhd
              subPath: shows/uhd
            - name: hddpool2-apps
              mountPath: /trash
              subPath: sonarr-uhd
            - name: hddpool2-apps
              mountPath: /data
              subPath: qbittorrent
      volumes:
        - name: sonarr-uhd
          persistentVolumeClaim:
            claimName: sonarr-uhd
        - name: hddpool2-apps
          persistentVolumeClaim:
            claimName: hddpool2-apps
        - name: hddpool2-media
          persistentVolumeClaim:
            claimName: hddpool2-media
      resources:
        limits:
          memory: 512Mi
        requests:
          cpu: 10m
          memory: 512Mi
