apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: server
resources:
  - cron-workflow.yaml
  - sealed-secret.yaml
helmGlobals:
  chartHome: ../../../helm/charts
helmCharts:
  - name: persistence
    valuesInline:
      app: rclone
      size: 128Mi
