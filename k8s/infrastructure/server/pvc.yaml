apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: server
spec:
  accessModes:
    - ReadWriteOnce
  resources:
     requests:
       storage: 32Gi
  storageClassName: ""
  volumeName: server
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Ti
  storageClassName: ""
  volumeName: media
